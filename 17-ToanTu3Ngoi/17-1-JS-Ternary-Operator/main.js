let number = -10;
let message = number >= 0 ? "Positive integer" : "negative integer";
console.log(message);

let number2 = 13;
let message2 = number2 % 2 === 0 ? "Solid Number" : "Cracked Number";
console.log(message2);
